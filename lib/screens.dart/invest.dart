import 'package:flutter/material.dart';

class Invest extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child:
              Text('I N V E S T', style: TextStyle(color: Colors.grey[200]))),
    );
  }
}
